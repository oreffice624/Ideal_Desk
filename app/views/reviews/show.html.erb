<div class="row">
    <ul>
        <li class="comment-create">
            <h3 class="text-left title">コメント</h3>
        </li>
  	    <li id="comments_area">
            <%= render partial: 'comments/index', locals: { comments: @comments } %>
	    </li>
    </ul>
    <hr>
    <% if user_signed_in? %>
        <div class="comment-create">
	        <h3 class="text-left">コメントを書く</h3>
	        <%= render partial: 'comments/form', locals: { desk: @desk, comment: @comment, review: @review } %>
	    </div>
    <% end %>
</div>